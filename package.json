{
  "devDependencies": {
    "newman": "^6.2.1",
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "infra": "docker-compose up -d",
    "backend": "mvn spring-boot:run",
    "frontend": "npm start --prefix ui",
    "dev": "npm run infra && concurrently -k -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\""
  }
}
